[pytest]
# 默认跳过 integration 标记的测试（可通过 -m integration 运行）
addopts = -m "not integration" -k "not (test_server_config or test_stock_codes)"
asyncio_mode = auto

# 标记定义，避免警告
markers =
    integration: 标记集成/端到端测试（默认跳过）
    asyncio: 标记 asyncio 测试

